// Shared between hero.tsx and intro-gate.tsx: both split into an artwork
// container plus a fixed-height strip below it, and those strips must stay
// exactly the same height or the gate-to-hero handoff visibly jumps.
export const SCROLL_CUE_STRIP_HEIGHT_CLASS = "h-14";
